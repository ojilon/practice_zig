# Questions — Hello and Build

1. What command runs your project’s tests through the build system?
   > Your answer:

2. Where in `build.zig` did you declare the executable and the test step?
   > Your answer:

3. Why is it useful to drive even small projects through `build.zig` instead of only `zig run file.zig`?
   > Your answer:

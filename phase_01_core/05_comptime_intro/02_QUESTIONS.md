# Questions — Comptime Intro

1. What runs at compile time vs runtime in your table + `Stack(T)`? Point at one line of each.
   > Your answer:

2. Why does the dojo require `comptime T: type` instead of `anytype` for public container APIs?
   > Your answer:

3. When is `StaticStringMap` preferable to `std.StringHashMap`, and when is it not?
   > Your answer:

4. What did `@typeInfo` let you do in `fieldCount`/`describe` that runtime code cannot?
   > Your answer:

5. Name one thing you will NOT do at comptime, and why (build time, binary size, debuggability).
   > Your answer:

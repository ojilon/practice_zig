# Questions — Slices, Arrays, Lists

1. What is the difference between `[N]T` (array) and `[]T` (slice) for ownership and copying?
   > Your answer:

2. Write the 3-line unmanaged `ArrayList` lifecycle (init/use/deinit) from memory. Where does the allocator appear?
   > Your answer:

3. What does `ensureTotalCapacity` buy you before a bulk fill, and when would you use `ensureTotalCapacityPrecise` instead?
   > Your answer:

4. Who owns the slice returned by `buildSquares`, and how does the caller free it?
   > Your answer:

5. What happened in your failing-allocator test — exact error, and how did you prove no partial state leaked?
   > Your answer:

6. Which Phase 2 structure will reuse this folder's ownership doc pattern?
   > Your answer:

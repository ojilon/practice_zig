# Questions — Packages & Modules

1. Where in `build.zig` did you declare the module, the executable, and the test step? (file + ~line or pasted snippet)
   > Your answer:

2. Why must cross-phase imports go through `build.zig` instead of relative `@import("../...")` paths?
   > Your answer:

3. What does your build option do, what is its default, and how does the code read it?
   > Your answer:

4. What happened when you cross-compiled (`-Dtarget=...`)? Exact command + outcome.
   > Your answer:

5. How long would adding a second importer of `p01_demo` take now, and what are the steps?
   > Your answer:

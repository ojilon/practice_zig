# Questions — Language Basics

1. When do you return `?T` versus `error!T`? Give one example of each from your code.
   > Your answer:

2. What does `errdefer` do that `defer` does not, and where did you use it?
   > Your answer:

3. Show one exhaustive `switch` over your tagged union. What happens at compile time if you add a variant and forget the arm?
   > Your answer:

4. Where did you narrow an error set instead of using `anyerror`, and why?
   > Your answer:

5. Which later phase will import parsing helpers like `tryParseU64`? (Hint: config/flags, format parsers.)
   > Your answer:

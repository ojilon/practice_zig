# Questions — Memory

1. What is the difference between a general-purpose allocator and an arena allocator in terms of lifetime?
   > Your answer:

2. Why do we often pass an `Allocator` as a function or struct parameter instead of using a global?
   > Your answer:

3. After fixing your example, does the GPA report zero leaks on the happy path?
   > Your answer:

4. Who owns the memory inside the struct you wrote, and when is it freed?
   > Your answer:
